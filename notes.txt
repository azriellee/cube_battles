tech stack:
frontend: react + vite
backend: nodejs + express

future functions:
1. live battles
2. calculate elo for players and functionality to join random rooms

next iteration:

1. first will be a sign in/up page for users (cookie that lasts forever or smth so that users dont need to keep signing in)
2. incorporate with google oauth
3. next page allows users to choose game mode (1. practice mode (generic timer, count into player stats?), 2. battle rooms (current functionality), 3. live battles (future iteration))
